apiVersion: v1
kind: Service
metadata:
  labels:
    app: user
  name: user-service
  namespace: fastfood
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: app
  type: ClusterIP