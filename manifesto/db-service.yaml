apiVersion: v1
kind: Service
metadata:
  labels:
    app: users-db
  name: users-db-service
  namespace: fastfood
spec:
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  selector:
    app: users-db
  type: ClusterIP