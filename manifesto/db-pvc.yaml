apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: db
  name: db-data
  namespace: fastfood
spec:
  storageClassName: local-storage 
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi 