apiVersion: v1
kind: Service
metadata:
  labels:
    app: users
  name: app-service-users
  namespace: fastfood
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: app
  type: {{ .Values.service.type | default "LoadBalancer" }}