apiVersion: v1
kind: Service
metadata:
  labels:
    app: users-app
  name: app-service-users
  namespace: fastfood
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: users-app
  type: {{ .Values.service.type | default "LoadBalancer" }}